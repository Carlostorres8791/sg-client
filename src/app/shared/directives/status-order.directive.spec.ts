import { StatusOrderDirective } from './status-order.directive';

describe('StatusOrderDirective', () => {
  it('should create an instance', () => {
    const directive = new StatusOrderDirective();
    expect(directive).toBeTruthy();
  });
});
